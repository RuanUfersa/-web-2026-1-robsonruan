version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build required for static HTML"
    build:
      commands:
        - echo "Static site deployment"
  artifacts:
    baseDirectory: front-end
    files:
      - '**/*'
      - '!node_modules/**/*'
      - '!.git/**/*'
  cache:
    paths: []